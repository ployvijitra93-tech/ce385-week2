บรรทัดที่ 1 (ประกาศฟังก์ชัน): สร้างฟังก์ชัน toGrade(score) รับค่าตัวเลขคะแนนเข้ามาประมวลผล

บรรทัดที่ 2–4 (ตรวจสอบข้อมูลขอบเขต): เช็กว่าคะแนนติดลบ (score < 0) หรือเกินร้อย (score > 100) หรือไม่ ถ้าใช่ให้ คืนค่าข้อความแจ้งเตือนทันทีโดยไม่นำไปตัดเกรด

บรรทัดที่ 5–11 (ตัดเกรดแบบลดหลั่น): ตรวจสอบคะแนนจากมากไปน้อยตามลำดับ หากเข้าเงื่อนไขไหนจะ คืนค่าเกรดทันทีแล้วจบฟังก์ชัน

>= 80 คืนค่า A

>= 75 คืนค่า B+

>= 70 คืนค่า B

>= 65 คืนค่า C+

>= 60 คืนค่า C

>= 55 คืนค่า D+

>= 50 คืนค่า D

บรรทัดที่ 12 (เกรดตก): ถ้าคะแนนไม่อยู่ในเงื่อนไขข้างต้นทั้งหมด (ต่ำกว่า 50) จะ คืนค่า F

บรรทัดที่ 15 (แสดงผลเดี่ยว): พิมพ์ผลลัพธ์ทดสอบคะแนน 78 ออกทางหน้าจอ (ผลลัพธ์ได้ B+)

บรรทัดที่ 17–20 (วนลูปทดสอบ): สร้าง Array testScores รวมคะแนนหลายๆ รูปแบบ (รวมถึงคะแนนติดลบและเกิน 100) เพื่อวนลูป for...of แสดงผลการตัดเกรดออกมาทุกกรณี