บรรทัดที่ 1 (ประกาศฟังก์ชัน): รับพารามิเตอร์ 5 ตัวสำหรับเช็กเงื่อนไข ได้แก่ inputUser (ชื่อผู้ใช้), inputPass (รหัสผ่าน), role (สิทธิ์), isActive (สถานะใช้งาน), และ age (อายุ)

บรรทัดที่ 2–3 (กำหนดค่าถูกต้อง): สร้างค่าคงที่ CORRECT_USER = "admin" และ CORRECT_PASS = "ce385pass" ไว้เปรียบเทียบ

บรรทัดที่ 5–7 (เช็กชื่อผู้ใช้และรหัสผ่าน): ใช้ตัวดำเนินการ || (OR) หากชื่อผู้ใช้ผิด หรือ รหัสผ่านผิด ตัวใดตัวหนึ่ง จะเด้งออกจากฟังก์ชันทันทีด้วยข้อความ "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"

บรรทัดที่ 9–11 (เช็กสถานะบัญชี): ถ้า isActive มีค่าเป็น false จะคืนค่า "บัญชีนี้ถูกระงับการใช้งาน"

บรรทัดที่ 13–15 (เช็กอายุ): ถ้า age น้อยกว่า 18 ปี จะคืนค่า "อายุไม่ถึงเกณฑ์"

บรรทัดที่ 17–22 (เช็กสิทธิ์ผู้ใช้งาน):

ถ้า role เป็น "อาจารย์" จะ คืนค่า "เข้าสู่ระบบสำเร็จ (สิทธิ์ผู้ดูแล)"

ถ้า role เป็น "นักศึกษา" จะ คืนค่า "เข้าสู่ระบบสำเร็จ (สิทธิ์ทั่วไป)"

บรรทัดที่ 24 (กรณีไม่ตรงสิทธิ์ใดๆ): หากผ่านทุกเงื่อนไขข้างต้นมาได้ แต่ role ไม่ใช่ทั้งอาจารย์และนักศึกษา จะคืนค่า "สิทธิ์การใช้งานไม่ถูกต้อง"

บรรทัดที่ 26–31 (แสดงผลทดสอบ): เรียกใช้ฟังก์ชัน login() พร้อมส่งค่าทดสอบ 6 กรณีครอบคลุมทุกเงื่อนไขเพื่อพิมพ์ผลลัพธ์ออกทางหน้าจอ Console