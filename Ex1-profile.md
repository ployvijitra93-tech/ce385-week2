บรรทัดที่ 1-6 (กลุ่มประกาศตัวแปร):

nickname : เก็บชื่อเล่นเป็นข้อความ

studentId : เก็บเลขรหัสนักศึกษา

age : เก็บอายุปัจจุบันเป็นตัวเลข

major : เก็บชื่อสาขาวิชา

enrolledSubjects : เก็บจำนวนวิชาที่ลงเรียน

remainingYears : เก็บจำนวนปีที่เหลือเรียน (เอาไว้ใช้คำนวณปีจบ)

บรรทัดที่ 8 (คำนวณปีจบ): เอาปี 2569 มาบวกกับ remainingYears เพื่อให้ได้ปีที่จะจบจริงโดยไม่ต้องพิมพ์ตัวเลขตายตัวลงไป

บรรทัดที่ 10-16 (สั่งแสดงผล): ใช้คำสั่ง console.log() ร่วมกับเครื่องหมาย Backtick (`) ดึงค่าจากตัวแปรทั้งหมดมาเรียงต่อกันให้เป็นรูปแบบบัตรแนะนำตัว